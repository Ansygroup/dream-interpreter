.vercel
node_modules
dist
*.tsbuildinfo
.DS_Store
.env.evolution
.indexnow-key.local
# deploy idempotency markers (local only)
.deploy-sha
.di-deployed-sha

# temp supabase anon key (never commit)
.supa-anon.tmp
.deploy-env-sha

# Dreamscope local secrets — drop your Google OAuth + OpenRouter key here;
# the self-complete cron enables Google + regenerates locales when present.
# NEVER commit this file.
.dreamscope-secrets

# Vercel env pull (may contain secret references) — never commit
.env.vercel.local
.env.local

.vercel
.env*
