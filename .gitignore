.vercel
node_modules
dist
*.tsbuildinfo
.DS_Store
.env.evolution
.indexnow-key.local
# deploy idempotency markers (local only)
.deploy-sha
.di-deployed-sha

# temp supabase anon key (never commit)
.supa-anon.tmp
